# dependencies
/node_modules

# env files — never commit real secrets/keys
.env
.env.local
.env.*.local

# Local SearXNG runtime config — contains a real secret_key; commit only
# searxng/settings.example.yml, never this file.
searxng/settings.yml

# OS/editor junk
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*

# Claude Code local overrides and unrelated plugin scratch data — not
# project source, not meant to be shared across machines/sessions.
.claude/settings.local.json
.claude/data/

# Playwright MCP verification screenshots/logs — scratch, not source.
.playwright-mcp/
